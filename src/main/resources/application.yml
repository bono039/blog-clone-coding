spring:
  jpa:
    # 전송 쿼리 확인
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    # 테이블 생성 후, data.sql 실행
    defer-datasource-initialization: true

# 애플리케이션 실행시 일래스틱 빈스토크에서 값 덮어쓰므로 datasource 항목 삭제
#  datasource: # DB 정보 추가
#    url: jdbc:h2:mem:testdb
#    username: sa

  h2: # H2 서버 활성화
    console:
      enabled: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
            scope:
              - email
              - profile


jwt:
  issuer: wendyhan03@gmail.com
  secret_key: study-springboot